# Gatekeeper — veto logic, thresholds, threat model

> TODO: Document the deterministic veto logic, hard limits (`STRIKE_FLOOR_MM`, force/torque ceiling, forbidden motion classes), authorization tiers, fail-safe behavior, and threat model. See README §4. Placeholder only — populated by a later directive.
